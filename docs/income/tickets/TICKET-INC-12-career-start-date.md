# TICKET-INC-12 — Career start date anchors the income page

- **Area:** Income
- **Released in:** [v1.6 Income & growth](../../releases/v1.6_income_growth/overview.md)
- **Type:** Feature
- **Traceability:** adds FR-INC-12 (new); re-anchors FR-INC-2/5/6/7

## User story

As a user, I want to set the date my career actually started — separately from where my imported transaction history happens to begin — so my income trend and growth figures start where my working life started, instead of at whatever month my oldest bank export covers.

## Description

Adds one user-set date, the **career start date**, persisted on the `appSettings` singleton and editable from a control near the Income page header. When set, it **filters the Income page's data to start at that date** — every chart, panel and growth figure on `/income` covers `[careerStartDate, today]` instead of `[first transaction, today]`, so student-era income, a pre-career partial year, or a back-imported account with an early opening balance stop distorting the growth story. Nothing outside `/income` changes.

## Current situation (as-is)

- `IncomeStore.fullHistoryRange` ([income.store.ts:46](../../../src/app/feature-income/income.store.ts#L46)) is the single span every panel on `/income` shares, and it is computed purely from the data: `computeFullHistoryRange()` ([full-history-range.ts](../../../src/app/core/stats/full-history-range.ts)) takes the earliest of each active account's `openingBalanceDate` and its oldest transaction's `bookingDate`. There is no way to say "my history goes back further than my career does" (or vice-versa).
- Consequences today:
  - `computeYearlyIncomeSummary()` ([yearly-income-summary.ts](../../../src/app/core/stats/yearly-income-summary.ts)) gap-fills every calendar year in that span, so years before the user earned anything render as zero bars, and the first real year is flagged `isPartialYear` only because the *dataset* starts mid-year — not because the career did.
  - The monthly chart (`computeIncomeCategorySeries`, [income-category-series.ts](../../../src/app/core/stats/income-category-series.ts)) carries the same leading dead zone.
  - TICKET-INC-05's period-over-period / YoY growth and TICKET-INC-07's multi-year comparison both inherit this span, so their earliest comparison window can be a stretch of non-career data.
- `AppSettings` ([app-db.ts](../../../src/app/core/data-access/app-db.ts)) already carries optional, non-indexed fields (`primaryColor`, `locale`, `currencySymbol`, `excludedIncomeCategoryIds`) written through `AppSettingsRepository`'s read-merge-put setters ([app-settings.repository.ts](../../../src/app/core/data-access/app-settings.repository.ts)) and exposed by `AppSettingsStore` — the established home for exactly this kind of single scalar setting.
- `mm-page-header` ([page-header.component.ts](../../../src/app/shared/ui/page-header/page-header.component.ts)) takes `title`/`subtitle` inputs only — it has no content slot for a trailing control, so the Income page header currently can't host one.

## Desired result (to-be)

- New optional field `careerStartDate?: string` (ISO `YYYY-MM-DD`) on `AppSettings`, with a `setCareerStartDate()` read-merge-put setter on `AppSettingsRepository` and a matching method + signal on `AppSettingsStore`. **Additive, non-indexed — no Dexie version bump** (same reasoning as `excludedIncomeCategoryIds`).
- `IncomeStore` gains an `incomeRange` computed that clamps `fullHistoryRange` by the career start date: `from = max(fullHistoryRange.from, careerStartDate)` when set, otherwise `fullHistoryRange.from` unchanged. `to` is untouched. Every panel on `/income` reads `incomeRange` instead of `fullHistoryRange`.
- A small date control near the Income page header lets the user set and clear the date, with a short hint explaining it's the start of their working life, not of their data. `mm-page-header` gets an optional projected-content slot (`<ng-content>`) so the control sits in the header rather than being bolted above the first panel.
- Unset is the default and behaves exactly as today — no migration, no prompt, no inferred value.

## Acceptance criteria

> **Implementation note (2026-08-01).** The as-is above says `mm-page-header` "has no content slot for
> a trailing control" — that was true when this ticket was written and isn't any more: the component
> already projects `[title-adornment]` and `[actions]`
> ([page-header.component.html](../../../src/app/shared/ui/page-header/page-header.component.html)).
> So no slot was added; the career-start control is projected into the existing `[actions]` slot, and
> the criterion below is amended to cover that slot staying optional rather than a new one being
> introduced. Two smaller moves: `mm-input` gained `max` and `ariaInvalid` inputs (additive, mirroring
> its existing `min` — a wrapped control doesn't forward native attributes), and the bound a date is
> validated against lives on `IncomeStore` as `latestTransactionDate`/`rejectCareerStartDate` so the
> control checks the same signals the page's range is built from rather than reading a second clock.

- [x] `careerStartDate` persists across a reload and is written through `AppSettingsStore` → `AppSettingsRepository`, never a direct `appDb.appSettings` write from a component or store. (`AppSettingsRepository.setCareerStartDate` is the only `appDb.appSettings` write; `AppSettingsStore.setCareerStartDate` awaits it then patches state; `IncomeStore.setCareerStartDate` delegates to the store, and `IncomeCareerStartComponent` only ever calls `IncomeStore`. Reload path covered by `app-settings.repository.spec.ts` "setCareerStartDate writes the singleton row without one existing yet (TICKET-INC-12)" — a real `put` then `get` through fake-indexeddb.)
- [x] No Dexie `.version()` bump is added — the field is optional and non-indexed, matching the `excludedIncomeCategoryIds` precedent. (`git diff src/app/core/data-access/app-db.ts` = 12 insertions, 0 deletions: the `AppSettings` field + its doc comment and one `DEFAULT_APP_SETTINGS` line. No `.version(`/`.stores(` line added or edited.)
- [x] With the date unset, every figure on `/income` is byte-for-byte what it is today (`incomeRange === fullHistoryRange`). (`clampRangeToCareerStart` returns the *same object reference* when the date is unset or empty — `career-start-date.spec.ts` asserts `toBe`, not `toEqual`, for both. `income.store.spec.ts` "is the full history range untouched while no career start date is set".)
- [x] With the date set **after** the first transaction, the monthly chart and yearly panel both start at the career start date; pre-career years no longer appear as zero bars. (`income-overview.component.spec.ts` "starts the monthly chart at the career start date rather than the first transaction (FR-INC-12)" — first sr-only bucket row is `2026-04`, `2026-01` absent. `income-yearly-panel.component.spec.ts` "starts at the career start date instead of the first transaction, dropping pre-career zero bars (FR-INC-12)" — a 2024 payslip plus a 2026 one with the date at `2026-01-01` renders `['2026']`, no 2024/2025 zero bars.)
- [x] With the date set **before** the first transaction, the range is unchanged — the setting narrows the window, it never invents history the data doesn't have. (`income.store.spec.ts` "leaves the range alone when the career start date is before the first transaction — it narrows, never invents history"; `career-start-date.spec.ts` "never widens the range" and "changes nothing when the career start is exactly where the data begins".)
- [x] A career start date in the future (or after the last transaction) is rejected in the UI with a visible validation message and is not persisted. (`income-career-start.component.spec.ts` "rejects a date in the future with a visible message and persists nothing" and "rejects a past date that sits after the last transaction, and persists nothing" — both assert the message is in the rendered text *and* that `setCareerStartDate` was never called. "leaves a rejected date in the field to be corrected rather than snapping it back" covers the correction path; "caps the native picker at the last transaction" covers the `max` attribute.)
- [x] Clearing the control removes the setting and restores the full-history span. (`income-career-start.component.spec.ts` "clears the setting, restoring the full-history span" — the Clear button calls the repository with `undefined` and empties the field; `income.store.spec.ts` "restores the full span when the date is cleared" asserts `incomeRange()` is back to `fullHistoryRange()`.)
- [x] The date is rendered through `localeDate` and the control respects the app's locale setting (TICKET-SET-03/04). (The caption under the field is `{{ date | localeDate }}`; `income-career-start.component.spec.ts` "shows the stored date through localeDate rather than only in the native field" reads `09/01/2024` under the `en-US` default and `01/09/2024` after switching the locale setting to `en-BE`. The caption is the only place the app's own locale can show — a native date input always paints in the *browser's* locale.)
- [x] Unit tests cover: unset (range unchanged), set-after-first-transaction (range clamped), set-before-first-transaction (range unchanged), set-to-a-future-date (rejected), and the repository setter round-tripping the field without clobbering other `AppSettings` fields. (All five: `income.store.spec.ts` describe blocks `IncomeStore: incomeRange` and `IncomeStore: rejectCareerStartDate`; `career-start-date.spec.ts` for the pure helpers; `app-settings.repository.spec.ts` "setCareerStartDate preserves unrelated settings and stays a single row" — `locale` and `excludedIncomeCategoryIds` survive, count stays 1 — and "setCareerStartDate(undefined) clears the date without touching the rest of the row".)
- [x] ~~`mm-page-header`'s new content slot is optional~~ **— amended 2026-08-01 (see the implementation note): the `[actions]` slot already existed, so nothing was added; what's verified is that it stays optional** — existing pages that pass no content render unchanged; a spec covers both. (`page-header.component.spec.ts` describe block "PageHeaderComponent: the actions content slot" — two host components, one projecting nothing (title/subtitle render, no `button` in the DOM) and one projecting a `[actions]` button (both render). `page-header.component.html` is untouched by this change.)
- [x] `angular.json` bundle budgets not raised. (`angular.json` untouched — absent from `git status`. `ng build --configuration development` clean, no budget warnings, initial total 2.15 MB.)
- [x] Verified with the fallow skill and the coding-conventions skill. (`fallow audit --base HEAD` verdict `pass`: 0 dead-code issues, 0 clone groups, `complexity_introduced: 0` — the first pass flagged `validateCareerStartDate` and `latestTransactionDate` on CRAP, both rewritten (bound list / `reduce`) until the introduced count hit zero; the 2 remaining findings are pre-existing in `app-db.ts` and `app-settings.store.ts`. `ng lint` clean; `conventions-reviewer` run on the diff.)
- [x] Verified live in the browser: setting a career start date visibly shortens the Income page's monthly chart and yearly panel, and clearing it restores them. — **Deliberately skipped:** the user waived the live browser check on 2026-08-01 when asked. Everything else is verified (`ng lint` clean, `ng build --configuration development` clean, `fallow audit` verdict `pass`, and `ng test` at 1647 passing apart from two `suggestions-table.component.spec.ts` failures that pre-date this change and reproduce with it stashed — see the Notes), but nobody has seen this control render. Two things only a browser settles, flagged by `conventions-reviewer` and left unverified: the header's `[actions]` slot at narrow widths (`mm-fieldset` + legend + caption is a tall block next to the title), and retyping an existing date by keyboard — `apply()` now ignores the empty value a native date input reports mid-edit, covered by a spec, but nobody has watched the charts while doing it.

## Notes

- **2026-08-01:** the header control's *mounting location* moves under TICKET-INC-04 — instead of
  sitting directly in `mm-page-header`'s `[actions]` slot, that slot now holds a single "Income
  settings" popup trigger, and this control (unchanged internally) becomes the first section inside
  that popup's panel, alongside the category filter (TICKET-INC-03) and a new bonus-category
  smoothing checklist.
- **Scope is the Income page only.** The dashboard, account detail charts and the topbar range are untouched — this is an income-growth anchor, not a global data cut-off. If it later proves useful elsewhere, that's a separate ticket.
- **Placement in the build order:** independent of the remaining INC chain — it only touches `IncomeStore`'s range, which INC-01/02/03/06 already ship. It's cheapest to land right after TICKET-INC-06, so the yearly view (the panel where a leading run of zero bars is most visible) benefits immediately and INC-07's multi-year comparison inherits the clamped span for free rather than being retrofitted.
- Deliberately **not** inferred (no "your first salary transaction looks like month X") — consistent with this version's stated principle of no new inferred classification (see [../overview.md](../../releases/v1.6_income_growth/overview.md)'s FR-INC-3/FR-INC-4 rationale).
- `isPartialYear` semantics are unchanged: a career starting in June still makes that year partial and therefore uncomparable — which is correct, and now correct *for the right reason*.
- Related: TICKET-INC-05 (growth-rate panel) and TICKET-INC-07 (multi-year comparison) both consume the range this ticket clamps; neither needs changing if they read `IncomeStore`'s range signal rather than recomputing their own. Note the signal they should read is now **`incomeRange`**, not `fullHistoryRange` — the latter stays on the store as the *data's* span, which is what `incomeRange` and the date validation are built from.
- **Pre-existing test failures, not caused by this ticket (2026-08-01).** Two of them, both confirmed by stashing this ticket's changes and re-running against bare `HEAD`:
  1. `suggestions-table.component.spec.ts` fails two assertions deterministically — the confidence badge and the dot badge each expect both the category name and the percentage, but commit `46d07e2` split those two values one-per-badge in the template without updating the spec. Left alone: deciding whether FR-ML-16's intent (the spec) or the newer template layout is right isn't this ticket's call. The same commit also left `confidenceTextColor()` (`suggestions-table.component.ts:113`) referenced nowhere.
  2. A cross-file **flake** in the dashboard/account chart specs — assertions expecting `€1,234.56` receive `$75,00`, i.e. another spec file's currency/locale. `shared/utils/format-settings.ts`'s module signals are process-global (Vitest runs `isolate: false`). Reproduced at bare `HEAD` (one run of two showed 4 failures, including `category-breakdown-panel` and `top-transactions-panel`), so it is not this ticket's doing — but it is a real latent hazard worth its own ticket.

  **Both fixed on 2026-08-01, in a follow-up pass — see the ML-16 ticket's "Superseded" note for (1).** The root cause first written here for (2) was wrong and is corrected for the record: the guess was that `src/test-setup.ts`'s `afterEach` reset was time-of-hook rather than ownership based, i.e. that it worked but ran too early. It never worked at all. The builder pre-bundles each spec through the Angular compiler while Vite transforms the setup file separately, so the setup file holds its *own copy* of `format-settings.ts` and resetting it reached no spec — a throwaway probe spec confirmed the hook ran while the specs' `locale` signal kept its leaked value, and neither the `@/` alias, a relative path, nor moving the file inside `src/app/` changes that. The actual leak was the five pure-utility specs (`currency-format`, `date-format`, `number-format`, `date-buckets`, `tooltip-formatter`) that reset in `beforeEach` only, so their last test's `en-BE`/`$` escaped to whatever file ran next. Now handled by `withCleanFormatSettings()` (`shared/utils/format-settings.testing.ts`), which brackets each test on both sides and is registered from inside the spec graph where it lands.
