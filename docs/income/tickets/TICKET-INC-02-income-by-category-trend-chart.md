# TICKET-INC-02 — Income-by-category trend chart

- **Area:** Income
- **Released in:** [v1.6 Income & growth](../../releases/v1.6_income_growth/overview.md)
- **Type:** Feature
- **Traceability:** adds FR-INC-2 (new)

## User story

As a user, I want an income-by-category trend chart on this page (one line/area per income category, bucketed at my chosen granularity), so I can see whether growth is coming from my salary or from elsewhere, rather than staring at one lumped-together income line.

## Description

The page's first real content: a multi-series trend chart (one line/area per income category) so growth by source is visible instead of one lumped income line.

## Current situation (as-is)

- `trend-buckets.ts`/`computeTrendBuckets()` **no longer exist** — the dashboard's trend chart was split into per-category income/expense series by TICKET-STAT-17. Its replacement, [category-composition-trend.ts](../../../src/app/core/stats/category-composition-trend.ts)'s `computeCategoryCompositionTrend()`, already returns `{ bucketKeys, incomeSeries, expenseSeries }` with one `CategorySeriesEntry { categoryId, name, color, values[] }` per category — but capped at the **top 5** categories per kind (selected once from a whole-range breakdown, no "Other" catch-all) and scoped to the topbar's `[from, to]`. That cap and range-scoping are exactly what this ticket needs to differ on.
- [category-breakdown.ts](../../../src/app/core/stats/category-breakdown.ts)'s `computeCategoryBreakdown()` splits by category but only for one `[from, to]` snapshot; `computeCategoryCompositionTrend()` calls it once per bucket rather than reimplementing the per-transaction rules.
- Transfer exclusion is **not** a raw `transferId != null` check anymore: [classify-for-stats.ts](../../../src/app/core/stats/classify-for-stats.ts)'s `classifyForStats()` is the single classification pipeline (range → `nullified` → zero-amount → savings movement → linked transfer, plus `neutral`-kind, joint-leg attribution and TICKET-STAT-11's signed netting), and every income/expense aggregate goes through it.
- [balance-trend-signals.ts](../../../src/app/feature-accounts/balance-trend-signals.ts) — not the chart components themselves — is the precedent for "always full-history data, the topbar range only drives the chart zoom window" (`computeFullHistoryRange` + `computeZoomWindow`), shared by `AccountBalanceChartComponent`/`NetWorthHistoryChartComponent`.
- Granularity is **chart-local state, not on `RangeStore`** (TICKET-STAT-15): `RangeStore` holds only `preset`/`from`/`to`, and each chart seeds its own `signal<Granularity>(pickGranularityForSpan(from, to))` fed by an `mm-granularity-picker`.
- `bucketKeysInRange`/`bucketDateBoundaries` live in [`@/shared/utils`](../../../src/app/shared/utils/date-buckets.ts), not in `core/stats/`.
- Chart theming goes through [`@/shared/echarts`](../../../src/app/shared/echarts/index.ts) (`resolveChartCategoricalColors`, `resolveChartAnimation`, `formatAxisTooltip`) since the v1.9 redesign — see [trend-chart-panel.component.ts](../../../src/app/feature-dashboard/components/trend-chart-panel/trend-chart-panel.component.ts).

## Desired result (to-be)

- New pure helper `computeIncomeCategorySeries(transactions, categoriesById, selectedCategoryIds, from, to, granularity, ownSavingsIbans, accountsById)` in `core/stats/income-category-series.ts`, gap-filled via `bucketKeysInRange`/`bucketDateBoundaries` from `@/shared/utils`, returning `{ bucketKeys: string[]; series: CategorySeriesEntry[] }` — reusing `category-composition-trend.ts`'s existing `CategorySeriesEntry` shape so FR-INC-04/05/08's consumers and the chart builders line up with the dashboard's.
  - It differs from `computeCategoryCompositionTrend()` on exactly three axes, which is why it's its own helper rather than a call into that one: **no top-5 cap** (every selected income category gets a series), **selection-parameterised** (`selectedCategoryIds`, FR-INC-3), and **full-history-scoped** rather than topbar-range-scoped.
  - It must **not** re-derive per-transaction rules: route every transaction through `classifyForStats()` (or reuse `computeCategoryBreakdown()` per bucket the way `computeCategoryCompositionTrend()` does) so transfer/nullified/savings/joint/signed-netting behaviour can't drift from the rest of the app.
- `IncomeOverviewComponent` computes `range = computeFullHistoryRange(accounts, transactions, todayIso())`, owns a chart-local `granularity = signal(pickGranularityForSpan(rangeStore.from(), rangeStore.to()))` with an `mm-granularity-picker`, and renders an `NgxEchartsDirective` stacked-area/multi-line chart (one series per selected category, coloured by `category.color` via `resolveChartCategoricalColors`), with `dataZoom` driven by `computeZoomWindow(bucketKeys, rangeStore.from(), rangeStore.to(), granularity())` — same chain as `balanceTrendSignals()`.
- Legend click toggles a category's line (native echarts behaviour, no extra code).

## Acceptance criteria

> **Implementation note (2026-07-30) — buckets are fixed to calendar months, no granularity picker.**
> The to-be above (and criterion 5 below) called for a chart-local `mm-granularity-picker` seeded by
> `pickGranularityForSpan`, mirroring TICKET-STAT-15's per-chart control. Built that way first, then
> changed on the product owner's call during review: **the whole Income page buckets monthly and the
> control is gone.** The reasoning it turned on — income is a monthly-cadence concept everywhere else
> in v1.6, so the picker offered four settings that made the page *harder* to read and one that
> didn't: FR-INC-05's growth rate is period-over-period and year-over-year, FR-INC-04 smooths an
> annual lump sum *across months*, and FR-INC-10's gross wage entries are keyed literally
> `yearMonth: 'YYYY-MM'`. At `day`/`week` a salary series is one spike per payday and flat elsewhere.
> `computeIncomeCategorySeries()` still takes `granularity` as a parameter — the constraint is the
> page's choice, not the helper's, so FR-INC-06's yearly view can pass `'year'`.
>
> **Follow-on, same review: the topbar range no longer scrubs this chart's zoom window either.**
> Monthly buckets made that rule actively harmful — the app's default `this-month` preset resolves
> to exactly *one* month bucket, so the page's headline trend chart opened on a single dot (caught
> in the live check, not by any test). The Income page now always opens on its full monthly history
> and leaves the `dataZoom` slider for the user to narrow by hand. This is a deliberate departure
> from TICKET-STAT-03's convention, which the account charts still follow: a growth trend is only
> meaningful across many periods, and the topbar is usually narrower than that. **Later FR-INC
> tickets should follow this page's rule, not STAT-03's.**

- [x] `computeIncomeCategorySeries()` returns one value per `bucketKeysInRange(from, to, granularity)` entry for every series (gap-filled, zero for buckets with no matching income); unit tests cover an empty range, a range with two overlapping income categories, and a category excluded via `selectedCategoryIds`. ([income-category-series.ts](../../../src/app/core/stats/income-category-series.ts); [income-category-series.spec.ts](../../../src/app/core/stats/income-category-series.spec.ts) → "returns no series and no buckets for an empty range with no selection", "zero-fills every bucket for a selected category with no income at all", "gives two overlapping income categories one aligned value per bucket each" (asserts an explicit `[150, 0, 90]` — February is a real 0, not a hole), "omits a category excluded from the selection entirely", plus "every series has exactly one value per bucket key, whatever the granularity" at `quarter`.)
- [x] Every income/expense decision goes through `classifyForStats()` (directly or via `computeCategoryBreakdown()`) — no local `transferId`/`nullified`/savings re-checks; unit test covers a linked transfer leg and a `nullified` transaction being ignored. (The helper never inspects a transaction at all — it delegates to `computeCategoryBreakdown()` once per bucket via the shared `computePerBucketBreakdowns()`, exactly as `computeCategoryCompositionTrend()` does. `income-category-series.spec.ts` → "ignores a linked transfer leg", "ignores a nullified transaction", "ignores a withdrawal from an own savings account rather than counting it as income", "nets a refund on an income category down rather than bucketing it as expense" (TICKET-STAT-11 signed netting: 2000 + (−200) = 1800).)
- [x] No top-N cap: a user with 7 selected income categories gets 7 series (the one behavioural difference from the dashboard's `computeCategoryCompositionTrend()`). (`income-category-series.spec.ts` → "applies no top-N cap: 7 selected income categories give 7 series" — asserts 7 series out, and in the same test asserts `computeCategoryCompositionTrend()` on identical input returns 5, so the difference is pinned side by side rather than asserted in isolation. Also covered at the chart layer by `income-overview.component.spec.ts` → "applies no top-N cap of its own: 7 series in, 7 series out".)
- [x] Chart renders one line/area per selected income category, coloured via `category.color`; deselecting a category (FR-INC-3) removes its line without recomputing the others' totals. (`buildIncomeTrendChartOption()` in [income-overview.component.ts](../../../src/app/feature-income/components/income-overview/income-overview.component.ts); `income-overview.component.spec.ts` → "renders one line/area series per selected category, coloured by the category colour". The "without recomputing the others' totals" half is asserted on the pure helper — `income-category-series.spec.ts` → "leaves the remaining categories' own totals untouched when one is deselected" — since each series is built from its own per-bucket category total and never from a share of a whole; confirmed live too, see the last criterion.)
- [x] Uses `computeFullHistoryRange`~~/`computeZoomWindow`~~ with ~~a chart-local granularity signal (TICKET-STAT-15's per-chart control — **not** a `RangeStore.groupBy`, which doesn't exist), so scrolling/zooming never drops data outside the topbar's currently selected range.~~ **a fixed `'month'` granularity and a zoom window that always spans the full history — both amendments in the note above; still not a `RangeStore.groupBy`, which doesn't exist.** (`range` = `computeFullHistoryRange(activeAccounts, transactions, todayIso())`, the module-level `INCOME_GRANULARITY = 'month'`, and a `zoomWindow` of `0 … bucketKeys.length - 1`. `income-overview.component.spec.ts` → "always opens on its full monthly history, ignoring a narrow topbar range" (sets a one-month `RangeStore` custom range and asserts both `dataZoom` controls still span every bucket), "buckets by calendar month regardless of the topbar range, so bucket keys are YYYY-MM", "offers no granularity control", and "seeds both dataZoom controls from the given window rather than trimming the data". Live proof in the last criterion.)
- [x] `angular.json` bundle budgets are not raised. (`angular.json` is untouched — not in `git status`. `ng build --configuration development` passes; echarts stays in this feature's lazy chunk because `INCOME_ROUTES` provides it at the route level, matching `DASHBOARD_ROUTES`/`ACCOUNTS_ROUTES` per TICKET-PERF-01.)
- [x] Verified live in the browser: chart shows separate lines for Salary and Other Income on seeded data; toggling a category off in the FR-INC-3 selector removes its line. (Dev server on :4210, `/income`, re-run after both amendments above. `getOption()` reported buckets `["2026-03" … "2026-07"]`, `dataZoom` `startValue: 0, endValue: 4` — the whole history open by default with the topbar still on `this-month` — and two `type: 'line'`, `stack: 'income'` series carrying each category's own stored colour: `Salary` `#34D399` `[0, 2800, 2800, 2800, 2800]` and `Other Income` `#2DD4BF` `[0, 0, 6.42, 0, 0]`, both named in `legend.data`, no `mm-granularity-picker` in the DOM. Unchecking "Other Income" dropped it from both `series` and `legend.data` while Salary's five values stayed identical; re-checking restored it.
  - **This check caught two real problems no test would have.** (1) The route was missing `provideEchartsCore({ echarts })`, so the page threw `NG0201: No provider found for InjectionToken NGX_ECHARTS_CONFIG` and rendered nothing — the unit tests passed throughout because the spec supplies that provider itself. Fixed in [income.routes.ts](../../../src/app/feature-income/income.routes.ts); a fresh navigation afterwards logged zero console errors. (2) Once buckets went monthly, the default `this-month` topbar preset zoomed the chart to a single bucket — the one-dot trend chart that prompted the full-history amendment above.)

## Notes

- Ships the **raw**, unsmoothed series. FR-INC-04's annual lump-sum smoothing wraps this same helper's output — build this one first so there's a real series to smooth.
- If, while building, the only differences from `computeCategoryCompositionTrend()` turn out to be parameters rather than logic, prefer widening that helper (an optional `topN`/`categoryIds` argument) over shipping a near-duplicate — fallow flags duplication, and the two would have to be kept in sync forever otherwise.
  - **How this landed (2026-07-30):** neither extreme. The difference isn't purely a parameter — `computeCategoryCompositionTrend()` *derives* its category ids by ranking a whole-range breakdown, this one *receives* them — so widening it would have meant an argument that changes where the ids come from, not just how many. Instead the two genuinely shared halves were factored out of `category-composition-trend.ts` and are now exported and used by both: `computePerBucketBreakdowns()` (the per-bucket `computeCategoryBreakdown()` loop) and `buildCategorySeries()` (category-id list → gap-filled `CategorySeriesEntry[]`). Each caller is left with only its own id-selection step. `fallow audit` reports 0 duplication clone groups.
  - Fallow also flagged one **introduced** duplicate at the chart layer — the `grid`/`xAxis`/`yAxis`/`dataZoom` shell shared with `net-worth-history-chart.component.ts`. Extracted to `bucketedZoomAxisOption()` in [`@/shared/echarts`](../../../src/app/shared/echarts/bucketed-axis-option.ts) and adopted by both callers, which is also the right home for it: every full-history bucketed chart has to keep that geometry identical for the topbar-scrubs-the-zoom-window behaviour to look the same.
