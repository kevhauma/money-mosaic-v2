# Money Mosaic — Code Review 2 Backlog (Overview)

Derived from [./code-review-dx-solid.md](./code-review-dx-solid.md) (DX / maintainability / SOLID review, 2026-07-07). Sibling to the [first review's backlog](../code-review/overview.md) (correctness/performance); `CR2-*` IDs trace back to the review doc. Most items here were fixed directly (no separate ticket file — the review item number plus the code link is the record); only six items got a full ticket. Each ticketed line links to a `tickets/TICKET-*.md` file carrying its own user story, description, as-is/to-be, and acceptance criteria.

The list below is in recommended build order, not grouped by review section:

> **[2026-07-14]** The un-ticketed lines still open below have been ticketed in the [Code Review 3 backlog](../v1.3_code_review/overview.md) — that folder is now the single place to pick up this work. The lines are kept here (with their ticket pointers) for traceability only. TICKET-CLEANUP-01 and TICKET-SOLID-01 stay owned here.

- [ ] [TICKET-CLEANUP-01](./tickets/TICKET-CLEANUP-01-fallow-verified-dead-code.md) — Remove Fallow-verified dead code and correct the stale first-review CR-6.2 advice (CR2-6.4) — independent, ~10 lines; corrects the stale advice everything else should stop citing — **[2026-07-14]** cross-check against CR3 review §5 first: the 2026-07-14 fallow run's unused-export/class-member findings were all verified false positives
- [x] [TICKET-TEST-01](./tickets/TICKET-TEST-01-orchestrator-store-specs.md) — Spec the four untested orchestrator stores (CR2-5.1) — safety net for the refactors below
- [x] [TICKET-SOLID-02](./tickets/TICKET-SOLID-02-type-import-domain.md) — Single-source the import domain unions (`signConvention`, `dateFormat`, `encoding`) (CR2-3.1/3.2) — small, independent
- [x] [TICKET-SOLID-03](./tickets/TICKET-SOLID-03-extract-transfer-cleanup-cascade.md) — Extract the shared transfer-cleanup cascade helper (CR2-4.2, CR2-4.3, also deletes the dead `DeleteAccountResult` alias) — small, independent
- [ ] De-duplicate rule field/operator display labels between `rule-form` and `rule-summary` (CR2-3.3) — **ticketed as [TICKET-CAT-05](../v1.3_code_review/tickets/TICKET-CAT-05-rule-label-single-source.md)**
- [ ] Schedule CR-6.1 (entity stores to `core/state`) before the next cross-store feature (CR2-4.1) — **ticketed as [TICKET-SOLID-05](../v1.3_code_review/tickets/TICKET-SOLID-05-entity-stores-to-core.md)**; the "next cross-store feature" has since shipped — fallow (2026-07-14) traces all 20 circular dependencies to this placement, and the interim `ownSavingsIbans` suggestion is superseded
- [x] [TICKET-SOLID-04](./tickets/TICKET-SOLID-04-wizard-declarative-reparse.md) — Replace the wizard's hand-rolled reparse plumbing with a declarative pipeline (CR2-2.2) — moderate, independent of the SOLID-01 split
- [ ] [TICKET-SOLID-01](./tickets/TICKET-SOLID-01-split-transactions-overview.md) — Split `TransactionsOverviewComponent` into filter bar + selection model (CR2-2.1, folds in CR2-6.2's pure-filter-function extraction; sequence with [TICKET-NG-02](../code-review/tickets/TICKET-NG-02-overview-input-binding.md)) — the largest ticket
- [x] Extract the categoryId-merge inside `ImportBatchesStore.commitImport` as a pure `applyCategoryUpdates` function (CR2-2.3, [import-batches.store.ts:16](../../src/app/feature-import/import-batches.store.ts)) — **verified done 2026-07-14**: the pure function exists and both merge sites use it
- [ ] Decide yes/no on a `withPersistedCrud` signal-store feature for the five persist-then-patch CRUD stores, after [TICKET-NG-04](../code-review/tickets/TICKET-NG-04-finish-with-archivable.md) proves the store-feature pattern out (CR2-4.4) — **ticketed as [TICKET-NG-08](../v1.3_code_review/tickets/TICKET-NG-08-with-persisted-crud-decision.md)**
- [ ] Extract `createConfirmState<T>()` for the delete-confirm scaffolding duplicated between `accounts-overview` and `categories-overview` (CR2-6.3, Fallow `dup:edd1ec44`) — **ticketed as [TICKET-NG-07](../v1.3_code_review/tickets/TICKET-NG-07-create-confirm-state.md)**
- [ ] Decide on codifying Fallow for recurring runs — a minimal `.fallowrc.json` covering the known false-positive families plus an identity baseline (CR2-6.5) — **ticketed as [TICKET-CLEANUP-02](../v1.3_code_review/tickets/TICKET-CLEANUP-02-fallow-config.md)**, with the concrete config contents supplied by CR3 review §5/§6
- [x] Fix stale doc paths left by the `docs/v1` → `docs/v1.0_foundation` reorg — CLAUDE.md's knowledge table, the `project-map` skill, the code-review backlog's sibling link (CR2-1.1) — **verified done 2026-07-14**: all three named locations now reference `v1.0_foundation` correctly (a leftover broken self-link in the first review's overview was fixed during verification); the phantom `TICKET-DX-01` name is retired
- [ ] Commit messages describe the actual diff (docs-only changes typed `docs:`, no claimed-but-unbuilt features), enforced via commitlint on the existing husky hook (CR2-1.2, `80af6be`/`1d97d49`) — **ticketed as [TICKET-DX-02](../v1.3_code_review/tickets/TICKET-DX-02-commitlint-conventional-commits.md)**
- [ ] Decide one version-folder naming scheme (`vX.Y_name` everywhere or plain `vX`) before the next milestone folder is created (CR2-1.3) — **ticketed as [TICKET-DX-03](../v1.3_code_review/tickets/TICKET-DX-03-version-folder-naming-scheme.md)**

## Definition of Done (applies to every ticket)

Per [../../CLAUDE.md](../../CLAUDE.md): `ng lint` + `ng test` + `ng build --configuration development` all pass, plus a live browser check for any UI-visible change. Dexie schema changes stay additive; the production bundle budget in `angular.json` is never raised.
